import Link from "next/link";
import { FileWarning, HeartPulse, Mail } from "lucide-react";

export const metadata = {
  title: "About",
};

export default function AboutPage() {
  return (
    <div className="mx-auto max-w-3xl px-4 py-12 sm:px-6">
      <header className="mb-12">
        <p className="text-xs font-semibold uppercase tracking-wider text-accent">About</p>
        <h1 className="font-display mt-2 text-3xl font-bold tracking-tight text-ink sm:text-4xl">
          Project & disclaimer
        </h1>
        <p className="mt-4 text-pretty text-lg text-ink-muted">
          The Disease Prediction System is an academic interface prototype. It demonstrates how
          symptom intake, patient context, and ranked outputs could look before a real machine
          learning model is connected.
        </p>
      </header>

      <div className="space-y-10">
        <section className="rounded-2xl border border-warn/30 bg-warn/10 p-6 sm:p-8">
          <div className="flex gap-4">
            <FileWarning className="h-8 w-8 shrink-0 text-warn" aria-hidden />
            <div>
              <h2 className="font-display text-xl font-semibold text-ink">Medical disclaimer</h2>
              <p className="mt-3 text-sm leading-relaxed text-ink">
                This application is for <strong className="font-semibold">educational and informational purposes only</strong>.
                It is <strong className="font-semibold">not</strong> a replacement for professional medical diagnosis,
                treatment, or emergency care. Always consult a qualified healthcare provider for
                health concerns.
              </p>
              <p className="mt-4 text-sm leading-relaxed text-ink">
                Predictions shown here are generated by <strong className="font-semibold">mock rules</strong>, not a
                validated clinical model. Do not use this tool to make decisions about medications,
                procedures, or urgency of care.
              </p>
            </div>
          </div>
        </section>

        <section className="rounded-2xl border border-ink/10 bg-surface p-6 shadow-card sm:p-8">
          <div className="flex gap-4">
            <HeartPulse className="h-8 w-8 shrink-0 text-accent" aria-hidden />
            <div>
              <h2 className="font-display text-xl font-semibold text-ink">What is implemented today</h2>
              <ul className="mt-4 list-inside list-disc space-y-2 text-sm text-ink-muted">
                <li>Searchable multi-select symptom picker with categories</li>
                <li>Age and gender fields for basic patient context</li>
                <li>Deterministic mock predictions with confidence scores</li>
                <li>Results and model comparison views aligned with a future API contract</li>
              </ul>
            </div>
          </div>
        </section>

        <section className="rounded-2xl border border-ink/10 bg-surface-2/80 p-6 sm:p-8">
          <h2 className="font-display text-xl font-semibold text-ink">Future work</h2>
          <p className="mt-3 text-sm leading-relaxed text-ink-muted">
            The next phase replaces mock data with API responses, integrates an XGBoost (or similar)
            classifier, and adds calibrated probabilities plus explainability (for example, per-symptom
            contributions). History and account features may follow once a backend exists.
          </p>
        </section>

        <section className="flex flex-col gap-4 rounded-2xl border border-accent/20 bg-accent/5 p-6 sm:flex-row sm:items-center sm:justify-between sm:p-8">
          <div className="flex gap-3">
            <Mail className="h-6 w-6 shrink-0 text-accent" aria-hidden />
            <p className="text-sm text-ink">
              Ready to try the flow? Start from the home page or jump straight into prediction.
            </p>
          </div>
          <Link
            href="/predict"
            className="inline-flex shrink-0 items-center justify-center rounded-xl bg-accent px-5 py-2.5 text-sm font-semibold text-accent-foreground shadow-card transition hover:brightness-105"
          >
            Open predictor
          </Link>
        </section>
      </div>
    </div>
  );
}
